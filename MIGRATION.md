# R13.17 到 R13.18

1. 导入 R13.18 为新配置，先保留旧配置副本以便比较。
2. 使用自己当前的 Surge 格式订阅，只修改 NodePool 的 policy-path。已填订阅副本无需这一步。
3. 启用后，“外部资源”中的主配置规则下载项应全部消失，只保留 NodePool；模块自己的外部资源不在此范围。
4. 更新 NodePool 并测试实际节点。若仍返回 500，查看 Sub-Store 本次下载对应的错误响应或脚本日志，不能仅靠节点列表判断刷新成功。

| 项目 | R13.17 | R13.18 |
|---|---|---|
| 配置文件 | 一个主配置 | 一个主配置 |
| 规则下载 | 29 个 jsDelivr 资源 | 0 个，全部写入主配置 |
| 订阅入口 | NodePool.policy-path | 保留一处 |
| 策略组 | 39 个 | 39 个 |
| 地区空源 | 可能被当作空组直连 | 含原生 REJECT，供可见组回退 Auto |
| Smart 空池严格断网 | 曾错误承诺 | 明确不保证，仍需进一步方案和真机验证 |
| DNS | 独立直连 DoH | 保留 |

无需重建 Sub-Store，不需新增转换脚本或分离配置。原有 Sub-Store 模块仍负责处理手机上的 sub.store 地址。节点连接和订阅服务是否可用需要实际验证。
