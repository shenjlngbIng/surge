# 升级到 R13.25

R13.25 将服务出口改为自动测速，并对可明确归区的服务限制候选节点。此次仓库发布包含此前 R13.24 的最低延迟入口和 R13.25 的地区修订。订阅入口仍叫“桔子”，Fail-Closed、原 Smart、Telegram/APNs 与 DNS/UDP 设计保留。

## 升级步骤

1. 保留手机上仍能工作的旧配置、真实订阅地址和外部资源缓存。
2. 通过 [主配置 URL](https://raw.githubusercontent.com/shenjlngbIng/surge/main/Surge.conf) 导入新版，确认顶部版本为 R13.25。
3. 搜索 `桔子 =`，仅替换 `policy-path=` 后的完整占位地址，保留 UDP 和统一测试参数。
4. 更新订阅，确认 NodePool 有真实节点，Auto/Fast 有健康节点；再确认 APNs.list 在内的 29 份外部规则均已加载。
5. 启用规则模式，选择 `Final → Proxy`、`Proxy → Fast`。旧的手动选择可能保留，需在界面确认。
6. 取消自动组的临时手动覆盖，完成一轮测速。检查 ChatGPT 等组的候选地区，再通过新的服务请求查看实际策略链。

更新外部资源不会替换旧主配置。无需删除 Fail-Closed、重装 Surge、清空缓存或改动已经可用的 Sub-Store 模块。

## 主要变化

| 项目 | 仓库原 R13.23 | R13.25 |
| --- | --- | --- |
| 策略组 | 40 个 | 41 个，新增 Fast |
| 主规则 | 142 条 | 157 条；新增 15 条、改向 1 条媒体地区例外 |
| Proxy 首选 | Auto | Fast；其他选项保留 |
| 服务组 | 多数手动 select，默认跟随 Proxy | 19 个独立 url-test，其中 10 个限制地区；Apple 默认直连保留 |
| URL 测试 | 地区组 600 秒、100 ms 容差 | Fast、服务组及严格地区源为 300 秒、0 容差 |
| 订阅测试参数 | 使用节点自身参数或全局默认 | 统一 Cloudflare HTTP 204、5 秒超时，保留 UDP 中继 |
| 地区入口 | 本地区失效可回退 Auto | 通用入口保留回退；受限服务直接复制严格节点，不使用外层回退 |
| 外部规则 | 29 份固定提交资源 | 文件、地址与匹配选项保留 |
| 注释及文档 | 原版本说明 | 配置注释精简，README 按当前实现重写 |

## 选点行为变化

- ChatGPT、Claude、Gemini 只在已识别的美、日、新、台候选中选择；不跟随总出口切到香港。
- HBO 按 HBO Max 的美、新、港、台范围选择；Bahamut 延续台湾片库取向。
- 受限服务没有合格节点时失败。通用地区入口仍可跨区回退，二者用途不同。
- 名称含多个现有地区标签或中转、转发、箭头标记的节点会被严格地区源排除。应确认实际出口后规范订阅名称，不通过改名冒充地区。
- 修改 Proxy 不会覆盖独立服务组；需要固定服务出口时，在相应服务组内临时选择合格节点，之后取消覆盖恢复自动选择。
- 0 容差优先最低测试值，可能增加 IP 切换。Smart 仍可在 Proxy 中选择，但不会自动替换所有服务组的算法。

如果从此前下载的 R13.25 文件升级，本次在精简注释的基础上为 Fast 增加 `hidden=1`，隐藏独立卡片。自动测速、Proxy 引用、Fail-Closed 及分流行为保持，文档和发布校验同步更新。

## 推送与外部资源

ApplePush 仍按 `Proxy → Auto → DIRECT` 工作；raw.githubusercontent.com 仍独立使用 Auto。保留 `include-all-networks=true` 和 `include-apns=true`，不能为了消除兼容性提示而关闭推送依赖。[Surge 接管参数](https://manual.nssurge.com/profile/general.html)

只有在资源和节点就绪后，才进行手机验收。必要时重建系统推送连接，分别在 Wi-Fi、蜂窝网络下锁屏接收 Telegram 新消息。查看资源页的当前状态、失败详情和更新时间，不把历史事件当作本次更新结果。

地区范围、账号/影片限制、排查方法和验证边界见 [README.md](README.md)。本版未连接私人节点或手机，不宣称服务解锁、UDP 和通知送达已实测成功。
