# 升级到 R13.22

本版将订阅源改名为“桔子”，同步节点池、Auto 和五个地区源的全部引用。名称更短，功能、订阅格式和分流行为不变。

## 操作步骤

1. 保留当前有效的订阅地址和 Sub-Store 模块。
2. 通过 [主配置链接](https://raw.githubusercontent.com/shenjlngbIng/surge/main/Surge.conf) 导入 R13.22。
3. 在文本配置中搜索 `桔子 =`，将这一行 `policy-path=` 后的完整占位 URL 替换为自己的 Surge 格式订阅地址。
4. 保存并加载订阅，确认 NodePool 列出真实节点，再检查原先手动选择的策略。

本版已经同步 7 处组引用，导入整份配置即可。只更新旧配置的外部资源不会改掉旧组名。

| 项目 | R13.21 | R13.22 |
| --- | --- | --- |
| 订阅源名称 | Subscription | 桔子 |
| 订阅入口数量 | 1 | 1 |
| 策略组数量 | 40 | 40 |
| 主分流指令 | 142 | 142 |
| 外部规则文件 | 29 | 29 |

规则仍固定到提交 `6e8e1bfbbdda66ee8ad0a5ad3979b6de8b5b7a51`，全部列表内容和地址保持不变。节点池、Smart、地区回退、隐藏辅助组、哨兵和 UDP 参数均保留。
