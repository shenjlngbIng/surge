# 升级到 R13.21

本版修正广告误拦和境外加密 DNS 的匹配顺序，保留原有 40 个策略组、29 份外置规则、单订阅入口、节点池和哨兵。辅助组继续隐藏，地区与服务组保持可见。

## 操作步骤

1. 备份正在使用的配置，记下当前有效的订阅地址、模块和手动选择的策略。
2. 通过 [主配置链接](https://raw.githubusercontent.com/shenjlngbIng/surge/main/Surge.conf) 导入新配置。
3. 搜索 `REPLACE_WITH_SURGE_SUBSCRIPTION_URL`，将完整占位 URL 替换为当前有效的 Surge 格式订阅地址。
4. 保留现有可用的 Sub-Store 模块，加载 Subscription 与 29 份规则，检查 NodePool 中的真实节点。
5. 启用新配置和规则模式，检查 Proxy、NodePool 以及各服务组的当前选择。

普通用户只需 Surge.conf，不需要导入维护 ZIP、关联分离文件或新增脚本。只点击“外部资源更新”不会升级旧主配置。主配置中的版本标记应为 R13.21。

## 与 R13.20 的区别

| 项目 | R13.20 | R13.21 |
| --- | --- | --- |
| 主分流指令 | 142 | 142 |
| Ads 活动条目 | 152 | 143 |
| 境外 DNS 853/8853 域名请求 | 先被端口规则拒绝 | 已列出的端点先进入 Proxy |
| 境外 DNS 53 端口请求 | REJECT | REJECT |
| 外部规则文件 | 29 | 29 |
| 策略组 | 40 | 40 |
| 可见与隐藏组 | 29 / 11 | 29 / 11 |
| 订阅入口 | Subscription | Subscription |

新配置的规则 URL 应含固定提交 `6e8e1bfbbdda66ee8ad0a5ad3979b6de8b5b7a51`。旧版链接已经固定到旧快照，反复刷新旧资源不会获得本次 Ads 修正，需要升级主配置并加载新的资源 URL。其余 28 份列表内容未变。

配置保留原有独立直连 DoH 和 UDP 参数。地区组仍可回退 Auto，服务及共享登录依赖的出口不变。此次修正不代表真实节点的 UDP、解锁或 DNS 检测已经通过实机验证。
