# 安全边界

R13.17 的公开配置不包含真实订阅、节点、令牌或私人日志。用户只在本机替换唯一的 `NodePool.policy-path` 占位地址。

## 节点与策略

- `[Proxy]` 保持为空，不得添加回环诊断、静态假代理或拒绝代理。
- `NodePool` 是唯一订阅入口；`Auto` 只递归导入真实节点。
- Proxy 没有 DIRECT 自动兜底；无可用节点时失败关闭。
- UDP 不支持时使用 REJECT，不回退 DIRECT 伪造可用。

## DNS

- AliDNS 与 DNSPod DoH 开启证书校验并直接完成节点启动引导。
- 应用内 DoH/DoT 域名继续固定进入 Proxy。
- 53 端口 DNS 被接管，53、853、8853 未审阅出口被拒绝。

## 供应链

29 个仓库规则资源固定到完整提交 `2b8fa93901061cf0482b079203630bcd11bfe0b1`。运行时不再加载动态国内补充或大型动态广告/钓鱼拒绝表。

安全问题请提交不含订阅 URL、令牌、节点凭据和私人日志的最小复现。
