# 安全边界

R13.20 的公开配置不含私人订阅、节点凭据或日志。仅在本地填写订阅；勿将私人配置提交到公开仓库。

外部规则使用固定提交的 GitHub raw URL。发布时校验 29 份文件的状态、内容哈希、语法和来源锁；下载仍依赖用户网络，不能保证任意网络环境的可达性。

应走代理的默认路径保持非空保护：隐藏订阅源和地区源含原生 REJECT，Smart 含不可连接的本机 HTTP 保护项，NodePool 默认选择 Smart。该保护不影响明确的国内、LAN、APNs 直连例外，也不会覆盖用户主动选择的 DIRECT、全局直连模式或其它模块的改写。

Surge 自身 DoH 独立直连并校验证书，以免解析代理服务器域名时出现循环。传统 DNS 可用于 DoH 引导和连通性检测；远端代理 DNS 也由服务端决定，不能承诺 DNS 与出口完全一致。

订阅导入启用客户端 UDP 开关，不会赋予服务端本来不支持的能力。UDP 不支持时保持拒绝，不自动转为 DIRECT。当前验证是静态检查、公开资源下载和离线行为模型，没有替代真机验收。

Pegasus 是历史 IOC 列表，不代表完整或实时的间谍软件防护。Security 默认 REJECT，可由用户主动修改；系统更新仍有独立作用。
