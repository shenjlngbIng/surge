# 安全边界

R13.18 的公开配置只保留一个 NodePool 订阅占位地址。私人订阅及节点凭据不进入公开仓库。

## 代理与哨兵

Auto 保留 Smart，仅从 NodePool 导入真实节点。Surge 官方说明空组可能使用 DIRECT/SUBSTITUTE，所以“没有显式 DIRECT”不等于“空组严格失败关闭”。本版不承诺完整自动断网哨兵；若整个节点池为空，应停止依赖它承载需要代理保护的流量。

五个隐藏地区源中的原生 REJECT 防止地区空源成为 DIRECT，不改变整个 Smart 空池的上述限制。没有回环诊断代理、代理链或静态节点注入。ApplePush 等既有明确 DIRECT 例外继续保留。

UDP 不支持时仍使用 REJECT。QUIC 与 STUN 仍按原配置处理。

## DNS

Surge 自身通过 AliDNS/DNSPod 直连 DoH 查询并校验证书。普通 DNS 可用于 DoH 域名引导和连通性检测；不能承诺所有 DNS 查询与代理出口一致，也不能保证任意应用自定义的加密解析器均被识别。

53 端口接管、53/853/8853 限制和已知应用 DNS 域名分流保留。PROTOCOL 中的 DNS 关键词仅适用于 Surge 自身发起的 DNS。

## 规则来源

29 个维护快照来自固定提交 `2b8fa93901061cf0482b079203630bcd11bfe0b1`。规则编译进主配置后，设备不再为这些文件访问 CDN。来源许可和限制见 NOTICE.md。

安全报告请遮住订阅 URL、认证信息、证书和私人日志中的凭据。
