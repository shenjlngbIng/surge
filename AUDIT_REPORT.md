# R13.22 配置复核

复核日期为 2026-09-08。比较基线为 R13.21，提交 `dc9440becc55ce282036176218295d147568b19f`。本版只缩短订阅源名称。

## 修改范围

- 将隐藏订阅源 `Subscription` 改为 `桔子`。
- 同步 NodePool、Auto 和五个地区源的 7 处 `include-other-group` 引用。
- 同步当前使用说明、检查工具、运行锁及发布文件。历史更新日志保留原版本的名称。

将基线配置中的组名和版本号替换后，与本版文件逐字节一致。全部 29 份外部规则内容、URL、顺序及匹配选项未变；主分流指令仍为 142 条，策略组仍为 40 个。

## 验证范围

- 检查唯一订阅入口、7 处引用、隐藏状态、保护成员及组引用无循环。
- 沿用地区筛选、空订阅、全部失效、单地区回退、手动节点选择及控制开关模型。
- 沿用 104 项配置故障注入，以及正常网站、广告、DNS 端口和行为退化测试。
- 校对来源锁、发布清单与文件校验和。

这些检查验证组名修改与原行为一致，不代表真实节点 UDP 或服务解锁经过手机实测。R13.21 的广告及 DNS 修正详情见[上一版复核记录](https://github.com/shenjlngbIng/surge/blob/dc9440becc55ce282036176218295d147568b19f/AUDIT_REPORT.md)。
