# R13.25 配置与发布复核

复核日期：2026-09-09。本轮以已发布 R13.25 提交 `37c84cf0c69840bcb4df01c5aab39fb8a6b16707` 为基线，处理国内哔哩哔哩 CDN 漏分流，并将文件头替换为指定的三行。下表同时记录当前版本保留的选点与保护设计。

## 1. 修改范围

| 内容 | 本次处理 |
| --- | --- |
| 服务选点 | 19 个服务组使用 url-test；其中 10 个限制地区来源，Apple 默认直连保留 |
| 全局选点 | 新增 Fast，作为 Proxy 的首选；Auto 保留原 Smart |
| URL 测试 | 300 秒有效期、0 容差、首次等待测试；订阅节点统一测试地址及 5 秒超时 |
| 地区边界 | 受限服务复制严格地区源节点，避免借外层地区回退进入全池；补充名称冲突与中转标签排除 |
| 流媒体例外 | 新增 15 条主规则，修改 viu.now.com 出口；区分 Hulu 美国站、TVer、香港 Now 等 |
| 故障保护 | Fail-Closed 原定义、Auto 哨兵、地区 REJECT、ApplePush 及下载备用路径保留 |
| B 站修复 | 保留原 16 条规则，新增 11 条 CDN 路由，国内规则前移至 Ads 前；国际站例外补充 SNI/Host 匹配 |
| 文件头与文档 | 仅保留作者、仓库、日期三行；README、MIGRATION、CHANGELOG 与来源说明同步 |
| 发布工具 | 维护既有审计门禁，同步 41 组、157 条规则、运行锁 schema 36、发布库存与校验和 |

与本轮基线相比，[General]、[Host]、[Proxy] 和全部 41 个策略组有效内容保持。Fast 继续隐藏，29 个组可见、12 个隐藏；自动测速、Fail-Closed、Telegram/APNs、DNS 与 UDP/QUIC 保持。仅 BiliBili.list 内容更新，另外 28 份规则文件保持；29 个引用统一切换到已发布的新快照。

## 2. 设计依据与限制

在现有五地节点分类中，ChatGPT、Claude、Gemini 使用美、日、新、台，HBO Max 使用美、新、港、台。巴哈台湾取向及 TikTok 保守候选属于明确的配置选择，未伪装成完整官方国家名单。通用服务和未拆分混合平台没有被宣称为“全世界均可解锁”。逐组依据及官方链接见 [README 的服务表](README.md#32-服务与地区候选)。

原截图中日本 211 ms 与 189 ms 相差 22 ms，香港 106 ms 与 99 ms 相差 7 ms，均可由原 100 ms 容差解释。Smart 还会依据真实连接和历史信息决策，不能用一个显示值判定所有请求选错节点。此次 url-test 明确设置 0 容差，但不把测试成绩当作服务解锁或带宽检测。[Surge 自动测试组](https://manual.nssurge.com/policy-groups/url-test.html)

Fail-Closed 是故意保留的本机 HTTP 哨兵，其失败显示为预期。独立直连 DoH、UDP 不支持时拒绝、APNs 接管与 DIRECT 备用均沿用既定设计。一般地区 fallback 可以跨区，受限服务不引用这些外层入口。

### B 站问题定位

离线按实际规则顺序复现：原版 `upos-hz-mirrorakam.akamaized.net`、`uposdash-302-bilivideo.yfcdn.net`、`szbdyd.com` 子域及部分 `bilicdn`/`hdslb` 域名命中 Final，默认经 Proxy。这是已确认的分流漏项，可能导致国内业务与视频出口不一致。

常见评论/API 域名 `api.bilibili.com`、`app.bilibili.com`、`grpc.biliapi.net` 原本已直连。当前广告快照未命中这些接口，因此没有把评论卡顿直接归因为广告拦截。修复补齐 CDN、前移国内规则并验证 API/图片/视频与国际站边界；不修改全局 DNS、IPv6，不封禁 PCDN，也不伪造真机播放结果。

共享 Akamai/YF CDN 仅添加指定主机；未整域放行共享 CDN，未复制共享 HTTPDNS 网段或不适用 iOS 的进程规则。来源见 [维护来源锁](Rules/maintained_sources.lock.json)。

## 3. 验证项目

| 检查层次 | 结果与覆盖 |
| --- | --- |
| 配置结构 | 41 个组、157 条主规则；策略引用、循环、唯一订阅、保护成员及选项检查通过 |
| 配置故障注入 | 119 项通过；包括恢复旧容差、删除哨兵、换入错误地区、引用跨区回退、变更测试参数及媒体改向 |
| 地区候选与故障 | 30 个正名称、21 个反例/歧义名称；147 项服务地区检查通过，覆盖空池、全失败、不合格节点仍健康及总出口手动覆盖 |
| 地区退化检验 | 向 ChatGPT/HBO 加入错误地区、使 Bahamut 引用 Auto 的 3 项退化均被检测 |
| 推送及资源恢复 | 11 项路径检查、1 项提前直连退化检验通过；保留手选坏节点时的 Auto 备用 |
| 域名分流 | 43 个域名/SNI 案例；含媒体目标与伪后缀反例 |
| 原有分流回归 | 16 个正常网站、8 个广告、132 个 DNS 端口、6 个配置损坏及 10 个行为退化案例通过 |
| 推送分流回归 | 27 个推送/下载域名、79 个 IP/端口、6 个 SNI 案例及 6 个退化案例通过 |
| 媒体退化检验 | Hulu 美国站、TVer、Now 被错误改向的 3 项退化均被检测 |
| B 站分流 | 202 项域名/SNI/IPv4/IPv6 形式、端口及共享 CDN 反例通过；3 项漏项/国际站 SNI/宽泛 CDN 退化均被检测 |
| 规则来源 | 26 份 RULE-SET、3 份 DOMAIN-SET，共 5,548 条有效内容；27 条 B 站规则与新固定快照同步 |
| 来源锁 | 1 份维护资源及 18 份服务来源锁检查通过；精确 China/Global 后缀集合无重叠 |
| 在线规则 | 29 份 GitHub raw 资源均返回 HTTP 200，内容与本地固定快照的 SHA-256 一致 |

规则模型展开后共有 5,676 条语义记录，仅在内存中核对，未把外部列表内嵌回主配置。网络资源、发布清单与校验和按仓库工作流继续核验；GitHub 上的具体运行结果以对应提交的 Actions 为准。

审计器先验证合法基线，再验证故障变体，避免“所有输入均失败”被误认为检查有效。此次更新的是与新行为对应的明确断言，没有删除、跳过或降低原有推送、DNS、来源和发布保护。

## 4. 发布一致性

规则快照 `4057ea435ece7617cabec5b7ac5a49c1d93937de` 先创建于仓库并验证可下载，随后主配置引用该提交；不存在尚未发布规则的自引用。运行锁记录当前配置 SHA-256、地区来源、自动选点模式及实际规则数量。

发布流程使用原有严格库存生成 RELEASE_MANIFEST.txt、SHA256SUMS.txt 和 SHA256SUMS_fixed.txt，校验清单与源码同步提交。完整归档仍由 tools/package_release.py 从相同库存生成；维护过程不将私人订阅或节点数据写入仓库。

| 主配置版本 | SHA-256 |
| --- | --- |
| 仓库 R13.23 基线 | `205b450a36605e1667cf76edd003838f6704f710f651648b6c366270c2668b02` |
| 此前交付的 R13.25 | `c017e4cc5b57ab6a2bb965111903c4f6564483bebfa966e6a48c52364f91e1fd` |
| 隐藏 Fast 前的 R13.25 | `75c852844344773ed95d0ec37096934ad487056c18607505b7221514c3ccc6ef` |
| 隐藏 Fast 后、B 站修复前 | `4a828c0def8b960e44419e29da9c60bfb5db964fd777e268b4290e9a09d0543f` |
| 本次 B 站修复与三行文件头 | `bddd4500a8ad256c980a34bfd29b0ceb9f1524b6749932969251437ce18d6325` |

## 5. 尚未进行的真机验收

环境中未运行 iOS Surge 内核，未连接用户私人订阅或手机。以上通过结果不代表 B 站真机卡顿已全部消失、实际节点已解锁、测速数据实时一致、UDP 成功或锁屏推送已恢复。

导入后需确认节点名称分类、当前策略选择、临时覆盖状态及所有外部资源，再通过实际请求检查出口。涉及登录、播放、账号地区或内容授权时，应验证对应服务；推送需要建立系统长连接并锁屏接收新消息。不存在可用候选时失败符合受限组设计。
