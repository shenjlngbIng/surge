# R13.19 配置复核

日期：2026-09-07。

## 已完成的配置修正

- 从 R13.18 恢复全部 29 个外部 RULE-SET / DOMAIN-SET 引用，原始规则列表未修改。改用本仓库固定提交的 GitHub raw URL，删除内嵌生成工具。
- 补齐空池保护链：Subscription 含 REJECT；Auto 含代理类型 Fail-Closed；NodePool 默认 Auto；隐藏地区源含 REJECT 并可回退 Auto。隐藏 Subscription 只提供成员，不直接承接业务流量。
- AdBlock、Security、Domestic 接到实际规则；UDP 接管 STUN 和未命中前置业务/国内规则的 UDP。
- 订阅导入显式启用 UDP 转发开关，保留不支持 UDP 时 REJECT 的行为。
- 保留独立直连 DoH 引导、证书校验、DNS 端口边界和 IP 规则 no-resolve；删除不参与当前自身 DNS 路由的五条 PROTOCOL DNS 规则。
- 保留 20 个服务组、五个地区、APNs 例外和既有 BiliBili/服务重叠修正。40 个组中仅增加一个隐藏订阅源；用户仍只改一个地址。

## 证据

- 29 个 GitHub raw URL 实际 GET 均返回 HTTP 200，SHA-256 均与固定快照一致。
- 主配置 144 条分流指令、29 个外部规则引用、0 条内嵌列表内容。
- 规则语义在内存中展开为 5,661 条；19 个域名/SNI 分流案例和 6 个资源破坏案例通过。
- 15 个地区名称、空订阅、全部失效、单地区可用、手动选择和开关行为的离线模型通过。
- 88 项配置故障注入通过。

## 验证边界

本环境没有运行 iOS Surge 内核，也没有连接用户真实代理。无法据此证明设备上的全局诊断页面、真实 UDP 服务、网站解锁或 DNS 检测结果一定成功。Fail-Closed 的失败是设计行为，不当作真实代理故障，也不当作成功的网络探针。

独立 DoH 不保证与代理出口同运营商。规则模式下的明确 DIRECT 与 ApplePush 的直连例外仍存在；哨兵不是关闭所有联网的系统级开关。用户已经处理好的 Sub-Store 不在本次修改范围内。
